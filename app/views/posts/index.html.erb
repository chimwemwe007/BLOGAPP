<<<<<<< HEAD
<% @title='My Blog | Posts' %>
<div class="mt-3 d-flex align-items-center flex-column">
  <div class="card mb-3 border-primary" style="width: 680px">
    <div class="row g-0">
      <div class="col-md-3">
        <img
          src="<%= @user.photo %>"
          class="img-fluid rounded-start"
          alt="..."
        />
      </div>
      <div class="col-md-4">
        <div class="card-body">
          <h2 class="card-title"><%= @user.name %></h2>
        </div>
        <div class="d-flex justify-content-end" style="width: 350px">
          <p class="card-text">
            <small class="text-muted"
              >Number of posts: <%= @user.posts_counter %></small
            >
          </p>
        </div>
      </div>
    </div>
  </div>

  <p class="card-text">
    <%= link_to "New Post", new_user_post_path(user_id: @user.id), class: 'btn
    btn-primary ', style: "width: 320px" %>
  </p>
  <h4>Posts</h4>
  <% if @posts.empty? %>
  <div class="card mb-3 border-danger" style="width: 540px">
    <div class="card-header bg-danger text-white">No Post Found</div>
    <div class="card-body">
      <h5 class="card-title text-danger">
        Please first submit a post then come back to this page thanks.
      </h5>
    </div>
  </div>
  <% else %> <% @posts.each do |post| %> <%= link_to user_post_path(id:
  post.id), class:'nav-link' do%>
  <div class="card mb-3 border-success" style="width: 540px">
    <h1 class="card-header bg-success text-white"><%= post.title %></h1>
    <div class="card-body">
      <p class="card-text"><%= post.text.slice(0,70)%> ...</p>
    </div>
    <!-- Lin to new Post  -->
    <div class="d-flex justify-content-end" style="width: 530px">
      <div
        class="card-footer d-flex gap-2 justify-content-end"
        style="width: 530px"
      >
        <p class="card-text">
          <small class="text-muted"
            >Comments: <%= post.comments_counter %></small
          >
        </p>
        |
        <p class="card-text">
          <small class="text-muted">Likes: <%= post.likes_counter %></small>
        </p>
      </div>
    </div>
    <% end %>
    <div>
      <!-- ADD new like  -->
      <div class="mt-3 d-flex align-items-end flex-column">
        <%= form_with model: @like, url: user_post_likes_path(post_id: post.id),
        class: 'form' do |f|%> <%= f.submit "Like", class:"btn btn-primary" %>
        <% end %>
      </div>
    </div>
    <div class="mt-3 mb-5 align-items-center flex-column">
      <h4>Add new Comment</h4>
      <%= form_with model: @comment, url: user_post_comments_path(post_id:
      post.id), method: :post, class: 'form' do |f|%> <%= f.text_area :text,
      class:"form-control" %>
      <br />
      <%= f.submit "Comment", class:"btn btn-primary" %> <% end %>
    </div>

    <h4>Recent Comments</h4>
    <% if post.most_recent_comments.empty? %>
    <div class="card mb-3 border-danger" style="width: 540px">
      <div class="card-body">
        <p class="card-text">No Comments Found</p>
      </div>
    </div>
    <% else %>
    <div class="card mb-3 border-secondary" style="width: 540px">
      <div class="card-body">
        <% post.most_recent_comments.each do |comment| %>
        <p class="card-text"><%= comment.author.name %>: <%= comment.text %></p>
        <% end %>
      </div>
    </div>
    <% end %> <% end %> <% end %>
  </div>
</div>
=======
<div class="card mb-3 text-center d-flex justify-content-center" style="max-width: 540px;">
  <div class="row g-0 text-center">
    <div class="col-md-4">
      <img src="<%= @user.photo %>" class="img-fluid rounded-start" alt="profile">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h4 class="card-title"><%= @user.name %></h4>
        <p class="card-text"><h6>Number of posts: <%= @user.posts_counter %></h6></p>
      </div>
    </div>
  </div>
</div>

 <%= link_to "Add New Post", new_user_post_path(user_id: @user.id), class:"btn btn-primary my-3" %>

  <h4>Posts</h4>
<h1 class='text-center mt-4'>All posts</h1>
<% @posts.each do |post| %>
<%= link_to user_post_path(user_id: @user.id, id: post.id), class:"nav-link mt-4 d-flex justify-content-center" do %>
<div class="card mb-3 text-center" style="max-width: 540px;">
  <div class="row g-0 text-center">
    <div class="col-md-8">
      <div class="card-body">
      <h2 class="card-title"><%= post.title %></h2>
        <h5 class="card-title"><%= post.text %></h5>
        <p class="card-text"><span>Comments: <%= post.comments_counter %></span></p>
      </div>
    </div>
        <% end %>
    <h6>Recent Comments</h6>
    <% if post.recent_comments.empty? %>
     <div class="card mb-3 border-secondary" style="width: 540px;">
       <div class="card-body">
            <p class="card-text">No Comments</p>
          </div>
        </div>
 <% else %>
     <div class="card mb-3 border-secondary" style="width: 540px;">
          <div class="card-body">
            <% post.recent_comments.each do |comment| %>
              <p class="card-text"><%= comment.author.name %>: <%= comment.text %></p>
              <% end %>
          </div>
      </div>
   

 <% end %>
 <% end %>

 <%== pagy_nav(@pagy) %>
>>>>>>> 289918409f164a7f36a95e0ae462e6cefa02984f
